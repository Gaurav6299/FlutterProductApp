import 'package:flutter/material.dart';

class trialbox extends StatelessWidget {
  const trialbox({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Row(
          
        ),
      ),
    );
  }
}